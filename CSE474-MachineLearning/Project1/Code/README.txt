Feed Forward Function:

We have used a feedForward function which takes in w1,w2,i as arguments and computes the hiddenOutput and output. 

This function was called in nnObjFunction and nnPredict Function.